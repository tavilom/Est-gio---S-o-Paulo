//analisando o pedido e mudando para que possa rodar em um codigo js puro, podemos ver que o código imprime o resultado de 91

let INDICE = 13;
let SOMA = 0;
let K = 0;

while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

console.log(SOMA);
